# CascLib.NET.IntegrationTests

This is only useful to run locally at this stage and requires a CASC storage on the local device.

Change values in `Configuration.cs` to match your system before running these.